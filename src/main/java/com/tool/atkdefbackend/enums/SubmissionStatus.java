package com.tool.atkdefbackend.enums;

/**
 * Submission status enum matching AnD.platform
 */
public enum SubmissionStatus {
    ACCEPTED,
    REJECTED,
    DUPLICATE,
    EXPIRED,
    OWN_FLAG,
    INVALID
}
